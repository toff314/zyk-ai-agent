# 数据运营AI平台

基于Vue3、FastAPI、LangChain和MCP的数据分析AI平台，为中药代煎平台和研发质量管理提供智能化的数据分析和代码审查能力。

## 项目简介

本平台是一个智能化的企业级AI数据分析系统，集成多种AI Agent能力：

- **数据分析Agent**：智能分析中药代煎平台的业务数据（医院订单、药品消耗、员工工作等）
- **研发质量分析Agent**：GitLab代码审查、提交统计、研发质量评估
- **通用对话Agent**：大模型通用问答和辅助对话

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Frontend)                      │
│  Vue 3.4 + TypeScript + Element Plus + Pinia + Vue Router    │
└─────────────────────────────────────────────────────────────┘
                              │ HTTP/REST API
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        后端层 (Backend)                        │
│         FastAPI + SQLAlchemy + LangChain + MCP                │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │   认证模块   │  │   对话模块   │  │      配置管理        │   │
│  │   JWT Auth  │  │   Chat API  │  │     Config API      │   │
│  └─────────────┘  └─────────────┘  └─────────────────────┘   │
│  ┌───────────────────────────────────────────────────────┐   │
│  │                  Agent 服务层                          │   │
│  │  ┌────────────┐  ┌──────────────┐  ┌──────────────┐  │   │
│  │  │ 数据分析    │  │ 代码审查      │  │ 通用对话      │  │   │
│  │  │ Agent      │  │ Agent        │  │ Agent        │  │   │
│  │  └────────────┘  └──────────────┘  └──────────────┘  │   │
│  └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ↓                    ↓                    ↓
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   SQLite    │      │   MySQL     │      │   GitLab    │
│   本地数据库  │      │   MCP服务    │      │   代码仓库    │
└─────────────┘      └─────────────┘      └─────────────┘
```

## 项目结构

```
zyk-ai-agent/
├── backend/                    # Python后端
│   ├── app/
│   │   ├── api/               # API路由层
│   │   │   ├── auth.py        # 认证相关API
│   │   │   ├── chat.py        # 对话相关API
│   │   │   ├── config.py      # 配置管理API
│   │   │   ├── conversations.py # 对话历史API
│   │   │   └── users.py       # 用户管理API
│   │   ├── agents/            # Agent实现层
│   │   │   └── prompts.py     # Prompt模板
│   │   ├── middleware/        # 中间件
│   │   │   └── auth.py        # JWT认证中间件
│   │   ├── models/            # 数据模型层
│   │   │   ├── database.py    # 数据库连接
│   │   │   ├── user.py        # 用户模型
│   │   │   ├── conversation.py # 对话模型
│   │   │   ├── message.py     # 消息模型
│   │   │   ├── gitlab_user.py # GitLab用户模型
│   │   │   └── config.py      # 配置模型
│   │   ├── services/          # 业务逻辑层
│   │   │   ├── agent_service.py # Agent服务
│   │   │   ├── gitlab.py      # GitLab集成服务
│   │   │   └── mcp_mysql.py   # MCP MySQL服务
│   │   ├── config/            # 配置管理
│   │   │   └── settings.py    # 应用配置
│   │   └── utils/             # 工具函数
│   │       └── security.py    # 安全工具函数
│   ├── mysql-mcp-server/      # MCP MySQL Server
│   │   ├── server.py          # MCP服务实现
│   │   └── test_server.py     # 测试脚本
│   ├── main.py                # 应用启动入口
│   ├── requirements.txt       # Python依赖
│   └── app.db                 # SQLite数据库文件
├── frontend/                   # Vue3前端
│   ├── src/
│   │   ├── api/               # API调用层
│   │   │   ├── auth.ts        # 认证API
│   │   │   ├── chat.ts        # 对话API
│   │   │   └── config.ts      # 配置API
│   │   ├── components/        # Vue组件
│   │   │   ├── MarkdownRenderer.vue # Markdown渲染组件
│   │   │   └── MentionPicker.vue   # @提及选择器
│   │   ├── views/             # 页面视图
│   │   │   ├── Login.vue      # 登录页
│   │   │   ├── Chat.vue       # 聊天主页
│   │   │   ├── Users.vue      # 用户管理页
│   │   │   └── Settings.vue   # 设置页
│   │   ├── router/            # 路由配置
│   │   │   └── index.ts
│   │   ├── store/             # Pinia状态管理
│   │   │   └── user.ts        # 用户状态
│   │   ├── types/             # TypeScript类型定义
│   │   │   └── index.ts
│   │   ├── utils/             # 工具函数
│   │   │   └── request.ts     # Axios封装
│   │   ├── App.vue            # 根组件
│   │   └── main.ts            # 应用入口
│   ├── public/                # 静态资源
│   ├── package.json           # 前端依赖
│   ├── tsconfig.json          # TypeScript配置
│   ├── vite.config.ts         # Vite配置
│   └── index.html             # HTML入口
├── tests/                     # 测试目录
│   └── test_mcp_mysql.py      # MCP测试
├── .gitignore                 # Git忽略配置
├── README.md                  # 项目文档
└── .clinerules/               # 开发规则文档
```

## 核心功能

### 1. 用户管理系统

**功能特性**：
- 用户登录/退出（基于JWT令牌认证）
- 管理员用户管理（添加用户、删除用户、重置密码）
- 基于角色的权限控制（admin角色 vs 普通用户）
- 密码加密存储（bcrypt哈希）

**访问控制**：
- `/users` - 仅管理员可访问
- `/settings` - 仅管理员可访问
- `/` - 所有认证用户可访问

### 2. 智能对话系统

**Agent类型**：

| Agent类型 | 应用场景 | 核心能力 |
|-----------|----------|----------|
| 数据分析Agent | 中药代煎平台数据分析 | 订单统计、药品消耗分析、员工效率评估、趋势分析 |
| 代码审查Agent | 研发质量管理 | GitLab提交记录查看、代码质量审查、研发统计 |
| 通用对话Agent | 日常问答辅助 | 通用问答、信息查询、辅助对话 |

**交互特性**：
- **@符号智能提及**：
  - 代码审查模式：快捷选择研发人员姓名
  - 数据分析模式：快捷选择数据库或表名
- **#符号提示词模板**：快速选择常用提示词模板
- **流式响应**：实时显示AI生成内容
- **对话历史**：保存和管理历史对话记录
- **Markdown渲染**：支持代码高亮和格式化显示

### 3. 配置管理系统

**支持配置项**：
- **AI模型配置**：API Key、API URL、Model ID
- **MySQL连接配置**：数据库主机、端口、用户名、密码、数据库名
- **GitLab配置**：GitLab地址、访问令牌

配置通过Web界面管理，支持实时更新。

## 快速开始

### 环境要求

**后端**：
- Python 3.9+
- SQLite 3（内置）

**前端**：
- Node.js 18+
- npm 或 yarn

### 后端启动

```bash
# 1. 进入后端目录
cd backend

# 2. 创建Python虚拟环境（推荐）
python3.14 -m venv backend/venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows

# 3. 安装Python依赖
pip install -r requirements.txt -i https://pypi.org/simple

# 4. 启动后端服务
python main.py
```

后端服务将在 **http://localhost:8000** 启动

- API文档（Swagger UI）：http://localhost:8000/docs
- API文档（ReDoc）：http://localhost:8000/redoc

**默认管理员账号**：
- 用户名：`admin`
- 密码：`admin123!`

> ⚠️ **注意**：生产环境请立即修改默认密码

### 前端启动

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装Node.js依赖（使用国内镜像加速）
npm install --registry=https://registry.npmmirror.com

# 3. 启动开发服务器
npm run dev
```

前端服务将在 **http://localhost:5173** 启动

### MCP MySQL Server（可选）

如需使用数据分析Agent连接MySQL数据库：

```bash
# 1. 进入MCP Server目录
cd backend/mysql-mcp-server

# 2. 配置环境变量
cp .env.example .env
# 编辑.env文件，填写MySQL连接信息

# 3. 启动MCP Server
python server.py
```

## 技术栈详解

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **FastAPI** | >=0.104.0 | 现代化Web框架，提供高性能API |
| **Uvicorn** | >=0.24.0 | ASGI服务器，用于运行FastAPI应用 |
| **SQLAlchemy** | >=2.0.0 | Python SQL工具包和ORM |
| **aiosqlite** | >=0.19.0 | 异步SQLite驱动 |
| **PyJWT** | >=2.8.0 | JWT令牌生成和验证 |
| **python-jose** | >=3.3.0 | JWT加密和签名 |
| **bcrypt** | >=4.0.0 | 密码哈希加密 |
| **LangChain** | >=0.1.0 | 大语言模型应用开发框架 |
| **langchain-openai** | >=0.0.2 | OpenAI模型集成 |
| **langchain-community** | >=0.0.10 | LangChain社区扩展 |
| **OpenAI** | >=1.6.1 | OpenAI官方SDK |
| **python-gitlab** | >=4.3.0 | GitLab API Python客户端 |
| **fastmcp** | >=0.1.0 | MCP (Model Context Protocol) 框架 |
| **pymysql** | >=1.1.0 | MySQL数据库驱动 |
| **pydantic** | >=2.5.0 | 数据验证和设置管理 |

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue** | ^3.4.21 | 渐进式JavaScript框架 |
| **TypeScript** | ^5.4.2 | JavaScript超集，提供类型安全 |
| **Vite** | ^5.1.6 | 下一代前端构建工具 |
| **Vue Router** | ^4.3.0 | Vue.js官方路由管理器 |
| **Pinia** | ^2.1.7 | Vue.js状态管理库 |
| **Element Plus** | ^2.5.6 | Vue 3 UI组件库 |
| **Axios** | ^1.6.7 | HTTP客户端 |
| **markdown-it** | ^14.1.0 | Markdown解析器 |
| **highlight.js** | ^11.9.0 | 代码语法高亮库 |
| **@vueuse/core** | ^10.9.0 | Vue Composition API工具集 |

## API接口文档

### 认证接口

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| POST | `/api/v1/auth/login` | 用户登录 | 公开 |
| POST | `/api/v1/auth/logout` | 用户登出 | 已认证 |
| GET | `/api/v1/auth/me` | 获取当前用户信息 | 已认证 |

### 用户管理接口

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | `/api/v1/users` | 获取用户列表 | 管理员 |
| POST | `/api/v1/users` | 创建用户 | 管理员 |
| PUT | `/api/v1/users/{id}` | 更新用户 | 管理员 |
| DELETE | `/api/v1/users/{id}` | 删除用户 | 管理员 |
| PUT | `/api/v1/users/{id}/reset-password` | 重置密码 | 管理员 |

### 对话接口

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | `/api/v1/chat/conversations` | 获取对话列表 | 已认证 |
| POST | `/api/v1/chat/conversations` | 创建新对话 | 已认证 |
| GET | `/api/v1/chat/conversations/{id}` | 获取对话详情 | 已认证 |
| DELETE | `/api/v1/chat/conversations/{id}` | 删除对话 | 已认证 |
| POST | `/api/v1/chat/send` | 发送消息 | 已认证 |
| GET | `/api/v1/chat/suggestions` | 获取@提及建议 | 已认证 |

### 配置接口

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | `/api/v1/config` | 获取配置 | 管理员 |
| PUT | `/api/v1/config` | 更新配置 | 管理员 |

## 前端路由

| 路径 | 组件 | 访问权限 | 描述 |
|------|------|----------|------|
| `/login` | Login.vue | 公开 | 登录页面 |
| `/` | Chat.vue | 已认证 | 聊天主界面 |
| `/users` | Users.vue | 管理员 | 用户管理页面 |
| `/settings` | Settings.vue | 管理员 | 配置管理页面 |

## 数据库表结构

### users - 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password_hash | VARCHAR(255) | 密码哈希（bcrypt） |
| role | VARCHAR(20) | 角色：admin/user |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### conversations - 对话表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| user_id | INTEGER | 关联用户ID（外键） |
| title | VARCHAR(200) | 对话标题 |
| agent_type | VARCHAR(50) | Agent类型 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### messages - 消息表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| conversation_id | INTEGER | 关联对话ID（外键） |
| role | VARCHAR(20) | 角色：user/assistant/system |
| content | TEXT | 消息内容 |
| created_at | DATETIME | 创建时间 |

### gitlab_users - GitLab用户缓存表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| gitlab_id | INTEGER | GitLab用户ID |
| username | VARCHAR(100) | GitLab用户名 |
| name | VARCHAR(100) | 用户显示名 |
| avatar_url | VARCHAR(500) | 头像URL |
| updated_at | DATETIME | 更新时间 |

### config - 配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| key | VARCHAR(100) | 配置键，唯一 |
| value | TEXT | 配置值（JSON字符串） |
| description | VARCHAR(500) | 配置说明 |
| updated_at | DATETIME | 更新时间 |

## 使用指南

### 数据分析Agent使用

**适用场景**：中药代煎平台业务数据分析

**功能能力**：
- 分析医院订单情况（订单量、订单金额、订单趋势）
- 统计药品消耗情况（使用量Top N、成本分析）
- 评估员工工作效率（处理量、准确率、时效性）
- 生成医院排名、地区患者排名
- 分析订单趋势变化（日/周/月趋势、季节性变化）

**操作步骤**：
1. 登录系统后，在Agent选择器中选择"数据分析"
2. 在输入框输入`@`符号，系统自动提示可用的数据库和表
3. 选择需要分析的数据库/表后，输入自然语言问题，例如：
   - "分析最近一周的订单趋势"
   - "统计本月药品消耗Top 10"
   - "评估各医院的订单情况排名"
4. 点击发送，AI将返回分析结果

**示例问题**：
- "最近7天每天的订单量和订单金额是多少？"
- "哪些药品使用量最高？"
- "哪个医院下单量最大？"
- "员工A本月处理了多少订单？"

### 代码审查Agent使用

**适用场景**：研发质量管理和代码审查

**功能能力**：
- 查看研发人员的提交记录
- 对指定提交进行详细的代码审查
- 生成代码质量报告（包括代码风格、潜在问题、优化建议）
- 统计研发人员的代码提交活跃度

**操作步骤**：
1. 在Agent选择器中选择"代码审查"
2. 输入`@`符号，系统自动提示GitLab上的研发人员列表
3. 选择需要审查的研发人员，可查看其提交历史
4. 选择特定提交（使用`@提交ID`）进行代码审查
5. AI将返回详细的审查报告

**示例问题**：
- "@张三 最近有什么提交？"
- "@abc123def 这次提交做了什么改动？"
- "审查一下 @abc123def 的代码质量"

### 通用对话Agent使用

**适用场景**：日常问答和辅助对话

**功能能力**：
- 通用知识问答
- 技术问题咨询
- 代码编写辅助
- 文档解释

**操作步骤**：
1. 在Agent选择器中选择"通用对话"
2. 直接输入问题或需求
3. AI将返回相应的回答

**示例问题**：
- "什么是闭包？"
- "帮我写一个快速排序算法"
- "解释一下Vue的响应式原理"

## 配置详解

### AI模型配置

在设置页面的"模型配置"中配置：

```json
{
  "api_key": "your-openai-api-key",
  "base_url": "https://api.openai.com/v1",
  "model": "gpt-4-turbo-preview"
}
```

| 参数 | 说明 | 必填 |
|------|------|------|
| api_key | OpenAI API密钥 | 是 |
| base_url | API基础地址（支持国内中转） | 否（默认OpenAI官方） |
| model | 模型名称 | 是 |

### MySQL配置

在设置页面的"数据库配置"中配置：

```json
{
  "host": "your-mysql-host",
  "port": 3306,
  "user": "your-username",
  "password": "your-password",
  "database": "your-database"
}
```

| 参数 | 说明 | 必填 |
|------|------|------|
| host | MySQL主机地址 | 是 |
| port | 端口号（默认3306） | 否 |
| user | 数据库用户名 | 是 |
| password | 数据库密码 | 是 |
| database | 数据库名称 | 是 |

### GitLab配置

在设置页面的"GitLab配置"中配置：

```json
{
  "url": "https://gitlab.example.com",
  "token": "your-gitlab-access-token"
}
```

| 参数 | 说明 | 必填 |
|------|------|------|
| url | GitLab实例地址 | 是 |
| token | GitLab个人访问令牌 | 是 |

> 💡 **提示**：GitLab访问令牌需要在GitLab用户设置中生成，需要`read_repository`和`read_api`权限。

## 开发指南

### 后端开发

**项目初始化**：
```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装开发依赖
pip install -r requirements.txt
```

**代码结构**：
- API路由放在 `app/api/` 目录
- 业务逻辑放在 `app/services/` 目录
- 数据模型放在 `app/models/` 目录
- 工具函数放在 `app/utils/` 目录

**运行测试**：
```bash
cd backend
pytest tests/ -v
```

### 前端开发

**项目初始化**：
```bash
npm install
```

**开发模式**：
```bash
npm run dev
```

**构建生产版本**：
```bash
npm run build
```

**代码规范**：
- 使用TypeScript进行类型检查
- 遵循Vue 3 Composition API最佳实践
- 使用Pinia进行状态管理
- 组件命名使用PascalCase

## 部署说明

### 生产环境部署建议

**后端部署**：
1. 使用Gunicorn + Uvicorn作为ASGI服务器
2. 配置Nginx作为反向代理
3. 使用环境变量管理敏感配置
4. 配置HTTPS加密
5. 设置定时备份数据库

**前端部署**：
1. 运行 `npm run build` 构建生产版本
2. 将 `dist/` 目录部署到Nginx静态服务器
3. 配置API代理

### Docker部署（推荐）

**后端Dockerfile**：
```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install -r requirements.txt

COPY backend/ .

CMD ["python", "main.py"]
```

**前端Dockerfile**：
```dockerfile
FROM node:18-alpine as builder
WORKDIR /app
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**Docker Compose**：
```yaml
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./app.db
      - JWT_SECRET_KEY=your-secret-key

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
```

## 安全建议

1. **修改默认密码**：首次登录后立即修改admin密码
2. **使用强密码**：密码至少8位，包含大小写字母、数字和特殊字符
3. **保护JWT密钥**：使用环境变量管理JWT_SECRET_KEY，不要硬编码
4. **启用HTTPS**：生产环境必须使用HTTPS
5. **限制API访问**：配置防火墙规则，限制API端口的访问
6. **定期备份**：定期备份数据库文件
7. **更新依赖**：定期更新依赖包，修复安全漏洞

## 常见问题

### Q1: 后端启动失败，提示数据库连接错误
**A**: 首次启动会自动创建SQLite数据库，如仍报错，检查是否有写入权限。

### Q2: 前端无法连接后端API
**A**: 检查后端服务是否正常启动，确认API地址配置正确（`frontend/src/utils/request.ts`）。

### Q3: AI响应很慢
**A**: 检查API配置中的`base_url`是否正确，网络是否稳定。可考虑使用国内API中转服务。

### Q4: @提及不显示选项
**A**: 确保已正确配置MySQL或GitLab，并刷新页面重新加载数据。

### Q5: 代码审查功能无法使用
**A**: 确认GitLab配置正确，访问令牌有足够的权限（至少需要read_repository权限）。

## 贡献指南

欢迎提交Issue和Pull Request！

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: add some amazing feature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 许可证

MIT License

## 联系方式

如有问题或建议，欢迎通过Issue联系。

---

**最后更新**: 2026-02-03
